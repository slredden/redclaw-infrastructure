# config/defaults.conf — Default values for infrastructure setup
# These are used as defaults by configure.sh. Override in system.conf.

TIMEZONE="America/Denver"
DEFAULT_USER="sredden"
LOCAL_NETWORK="192.168.10.0/24"
TAILSCALE_SUBNET="100.64.0.0/10"
GNOME_SESSION="ubuntu"
INSTALL_TAILSCALE=true
INSTALL_OPENCLAW_PREREQS=true
INSTALL_FIREWALL=true
REMOVE_XRDP=true
REMOVE_LIGHTDM=true
RDP_MODE="console"
